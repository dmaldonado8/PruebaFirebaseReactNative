interface InterfazEstilos {
  fontSize: number;
  padding: number;
  height: number;
  borderRadius: number;
}

const estilos: InterfazEstilos = {
  fontSize: 15,
  padding: 5,
  height: 45,
  borderRadius: 5,
};

export default estilos;
